<% if flash.any? %>
  <div class="row">
    <div class="col-xs-12">
      <% flash.each do |type, msg| %>
        <%= content_tag :div, msg, class: "flash #{type}" %>
      <% end %>
    </div>
  </div>
<% end %>
