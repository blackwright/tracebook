<%= form_for [commentable.author, commentable, commentable.comments.build],
    id: "write-comment" do |f| %>
  <%= f.text_area :body, placeholder: "Write a comment", rows: 3 %>
  <%= link_to "Cancel" %>
  <%= f.submit "Comment", class: "btn btn-md btn-primary pull-right" %>
<% end %>
